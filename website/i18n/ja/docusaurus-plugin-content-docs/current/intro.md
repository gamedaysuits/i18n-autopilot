---
sidebar_position: 1
slug: /
title: "はじめに"
---
# i18n-rosetta

完全にカスタマイズ可能な国際化（i18n）フレームワークです。1つのコマンドでロケールファイルを翻訳します。1つの設定で、すべてのメソッド、モデル、言語ペアを制御します。組み込みのメソッドで不十分な場合は、独自のメソッドを構築し、その動作を証明してデプロイすることができます。

```bash
npx i18n-rosetta sync
```

rosettaは、ロケールファイル、フォーマット、ターゲット言語を自動検出します。不足しているものを翻訳し、完了しているものをスキップし、すべての結果を検証して、クリーンな出力を書き出します。これがスタートラインです。

---

## なぜ自分でスクリプトを書かないのか？

各キーに対してGoogle Translateを呼び出す簡単なループを書くことは可能です。ほとんどの開発者がそうしており、約30行のコードで済みます。しかし、以下のような問題が発生します。

- **変更検出がない:** 英語の文字列を更新しても、翻訳は永遠に古いままです。rosettaはすべてのソース値をSHA-256ハッシュで追跡し、変更されたものだけを再翻訳します。
- **バッチ処理がない:** 1キーにつき1回のAPI呼び出しを行うと、200キーで200回の通信が発生します。rosettaはインテリジェントにバッチ処理を行います（設定可能、デフォルトはLLMで30キー/バッチ、Googleで128キー/バッチ）。
- **品質ゲートがない:** 機械翻訳はハルシネーションを起こしたり、ソースをそのまま返したり、間違った文字体系で出力したりします。rosettaは書き込む前にすべての翻訳を検証し、文字体系の誤り、不自然な長さの増加、ソースのそのままの出力などを検知して拒否します。
- **フォーマット認識がない:** JSONにハードコードされていませんか？rosettaは、JSON、TOML、YAML、Hugo Markdown（フロントマター＋本文）を自動検出して処理します。
- **メソッド制御がない:** すべての言語ペアで同じメソッドが使用されてしまいます。rosettaを使用すると、フランス語にはGoogle Translate、日本語にはLLM、Creeにはコミュニティがホストするカスタムパイプラインを、同じ設定ファイル内で使い分けることができます。

rosettaは、そのスクリプトのプロダクション版です。

---

## 何が違うのか

### すべてのメソッドがプラグイン

翻訳メソッドは**言語ペアごとに設定可能**です。Google Translate、LLM、コーチングされたプロンプト、カスタムAPIを同じプロジェクト内で組み合わせることができます。

```json title="i18n-rosetta.config.json"
{
  "version": 3,
  "pairs": {
    "en:fr": { "method": "google-translate" },
    "en:ja": { "method": "llm", "model": "google/gemini-2.5-pro" },
    "en:crk": { "methodPlugin": "crk-coached-v1" }
  }
}
```

フランス語にはGoogle Translate（高速、安価）を使用します。日本語にはプレミアムなLLM（ニュアンスを考慮）を使用します。Plains Creeには、文法規則、辞書、形態素検証を備えたコーチング済みプラグインを使用します。同じ `sync` コマンド、同じ品質ゲート、同じCLIを使用します。

### 証明する

あなたのメソッドが英語からスペイン語へ、トルコ語からアゼルバイジャン語へ、あるいは英語からCreeへ翻訳できると思いますか？

**それを証明してください。** 付属の[評価ハーネス](/docs/eval/harness)は、再現性のあるフィンガープリント付きのスコアリングで、あらゆる翻訳メソッドをベンチマークします。[リーダーボード](/leaderboard)はすべての提出を追跡します。

評価ハーネスとプロダクションCLIは、同じプラグインインターフェースを共有しています。ハーネスで高スコアを獲得したメソッドは、その言語を使用するコミュニティの同意があれば、プロダクション環境で使用できます。先住民の言語や低資源言語において、その同意は重要です。[Data Sovereignty](/docs/guides/data-sovereignty)を参照してください。

```bash
# Benchmark your method (in the eval harness repo)
cd gds-mt-eval-harness
python eval/baseline_experiment.py --dataset data/edtekla-dev-v1.json --submit

# Use it locally
npx i18n-rosetta sync
```

同じプラグインです。組み込んでテストしてください。

### 完全なツールキット

rosettaは単なる `sync` ではありません。完全なi18nパイプラインです。

| コマンド | 機能 |
|---------|-------------|
| `sync` | 不足しているキー、古いキー、フォールバックキーを翻訳する |
| `watch` | ソースファイル変更時に自動同期する |
| `lint` | ソースコード内のハードコードされた文字列をスキャンする |
| `wrap` | ハードコードされた文字列を `t()` 呼び出しで自動ラップする |
| `audit` | 未翻訳の `[EN]` フォールバック値をすべて一覧表示する |
| `integrity` | プレースホルダーの破損やエンコーディングの問題を検出する |
| `seo` | hreflangタグ、サイトマップ、JSON-LDを生成する |
| `status` | ペア設定、プラグイン、ベンチマークスコアを表示する |
| `provenance` | 翻訳リソースのライセンスを監査する |
| `plugin` | メソッドプラグインをインストール、削除、一覧表示する |

これらのうち3つ（`lint`、`sync`、`audit`）は、ハードコードされた文字列を捕捉して翻訳し、不完全なロケールがある場合はビルドを失敗させるCIパイプラインを形成します。

---

## アリーナ

[Method Leaderboard](/leaderboard)はスコアボードです。すべての提出物はGitコミットにフィンガープリントされ、特定のデータセットにバージョン付けされ、同じハーネスによってスコアリングされます。誰でも提出できます。

**あなたは何を証明できますか？** ハーネスはJSONを受け取ります。プラグインもJSONを受け取ります。JSONを生成するメソッドであれば、何でもテスト可能です。

| アプローチ | 例 |
|----------|---------|
| **コーチング済みLLM** | 最先端モデルのプロンプトに文法規則と辞書を注入する |
| **ファインチューニング済みモデル** | パラレルテキストでオープンモデルをトレーニングする（ただし評価データは除く） |
| **FSTゲートパイプライン** | LLMが生成 → 有限状態トランスデューサー（FST）が形態素を検証 → 再試行する |
| **チェーンモデル** | モデルAが下書き → モデルBがポストエディット → モデルCがスコアリングする |
| **辞書 + LLM** | 辞書から既知の用語を強制し、残りをLLMに処理させる |
| **進化的アルゴリズム** | 候補を生成し、スコアリングし、最良のものを変異させ、繰り返す |
| **部分翻訳** | サンプルを手動で翻訳し、LLMが一致することを証明し、残りを自動翻訳する |

モデルをファインチューニングする。進化的アルゴリズムをデプロイする。語学試験の学生の回答をテストする。ルックアップテーブルを構築する。3つのモデルをチェーンさせる。あなたのメソッドがJSONを生成する限り、ハーネスはそれをスコアリングし、フレームワークはそれを実行します。

:::danger 唯一のルール
**評価データでトレーニングしないでください。** ベンチマークデータセットにさらされたメソッドは失格となります。好きなものでファインチューニングしてください。ただし、テストセットだけは避けてください。
:::

これは公開招待状です。研究者、コミュニティメンバー、学生、あるいは単に関心がある人として、低資源言語に取り組んでいるなら、メソッドを構築し、ハーネスを実行して、トップスコアを獲得してください。問題は未解決です。インフラはここにあります。

**[→ リーダーボードを見る](/leaderboard)**

---

## 次のステップ

**はじめに:**
- [Installation](/docs/getting-started/installation) — 2分でセットアップする
- [Quick Start](/docs/getting-started/quick-start) — 初めての同期を実行する
- [Supported Languages](/docs/reference/supported-languages) — 標準で利用可能なもの

**セットアップのカスタマイズ:**
- [Translation Methods](/docs/guides/translation-methods) — ペアごとに適切なメソッドを選択する
- [Configuration](/docs/getting-started/configuration) — 完全な設定リファレンス
- [Hugo Multilingual Site](/docs/tutorials/hugo-multilingual-site) — Markdownコンテンツの翻訳

**さらに深く:**
- [Data Sovereignty](/docs/guides/data-sovereignty) — OCAP、CARE、およびMāoriデータ主権の原則
- [Support a Low-Resource Language](/docs/guides/low-resource-languages) — すべての始まりとなった課題
- [Cookbook: FST-Gated Pipeline](/docs/tutorials/fst-gated-pipeline) — 分解パイプラインを構築する
- [MT Evaluation](/docs/eval/) — ハーネスとリーダーボードの仕組み
- [Method Leaderboard](/leaderboard) — ライブスコアと提出物