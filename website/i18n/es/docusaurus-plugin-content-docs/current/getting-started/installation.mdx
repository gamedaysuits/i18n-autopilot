---
sidebar_position: 1
title: "Instalación"
---
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Instalación

## Requisitos

- **Node.js 20.11+** (ESM nativo)
- Una clave de API de traducción (consulte a continuación)

## Instalar

<Tabs>
  <TabItem value="npm" label="npm" default>

```bash
npm install --save-dev i18n-rosetta
```

  </TabItem>
  <TabItem value="npx" label="npx (no install)">

```bash
npx i18n-rosetta sync
```

  </TabItem>
  <TabItem value="pnpm" label="pnpm">

```bash
pnpm add -D i18n-rosetta
```

  </TabItem>
  <TabItem value="yarn" label="yarn">

```bash
yarn add --dev i18n-rosetta
```

  </TabItem>
</Tabs>

## Obtener una clave de API

Rosetta necesita un backend de traducción. Elija uno:

| Proveedor | Variable de entorno | Ideal para |
|----------|---------------------|----------|
| **OpenRouter** (recomendado) | `OPENROUTER_API_KEY` | Proyectos con mucho contenido, Markdown, más de 200 modelos |
| **Gemini** (nivel gratuito) | `GEMINI_API_KEY` | Inicio sin costo, buena calidad |
| **OpenAI** | `OPENAI_API_KEY` | Acceso directo a GPT-4o |
| **Anthropic** | `ANTHROPIC_API_KEY` | Acceso directo a Claude, texto con matices |
| **DeepL** | `DEEPL_API_KEY` | Idiomas europeos, formalidad integrada |
| **Google Translate** | `GOOGLE_TRANSLATE_API_KEY` | Pares clave-valor de alto volumen, más de 130 idiomas |
| **Microsoft Translator** | `MICROSOFT_TRANSLATOR_API_KEY` | Azure, más de 100 idiomas |
| **LibreTranslate** | `LIBRETRANSLATE_API_URL` | Autohospedado, prioridad en la privacidad |

<Tabs>
  <TabItem value="openrouter" label="OpenRouter (recommended)" default>

[OpenRouter](https://openrouter.ai) proporciona una API unificada para más de 200 modelos LLM. Nivel gratuito disponible.

```bash
# Sign up at https://openrouter.ai, then:
export OPENROUTER_API_KEY=sk-or-v1-...
```

O agregue a `.env.local`:

```bash
OPENROUTER_API_KEY=sk-or-v1-your-key-here
```

Ideal para: proyectos con mucho contenido, traducción de Markdown y proyectos que necesitan protección consciente del contenido (bloques de código, shortcodes, variables de interpolación).

  </TabItem>
  <TabItem value="google" label="Google Translate">

```bash
export GOOGLE_TRANSLATE_API_KEY=...
```

Ideal para: pares de cadenas clave-valor de alto volumen (más de 130 idiomas). **No recomendado** para contenido Markdown: Google Translate no tiene conocimiento de bloques de código, shortcodes ni variables de interpolación.

:::tip Detección inteligente
Si solo se establece `GOOGLE_TRANSLATE_API_KEY` (sin clave de OpenRouter), rosetta cambia automáticamente a Google Translate. No se necesita ningún cambio de configuración.
:::

  </TabItem>
</Tabs>

## Verificar instalación

```bash
npx i18n-rosetta --help
```

Debería ver la lista de comandos disponibles. Ya está listo para el [Inicio rápido](/docs/getting-started/quick-start).